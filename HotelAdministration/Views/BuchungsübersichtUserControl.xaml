<UserControl x:Class="HotelAdministration.Views.BuchungsübersichtUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HotelAdministration.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="800"
             DataContext="{Binding Buchungen, Source={StaticResource Locator}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Buchungsübersicht" FontSize="22" VerticalAlignment="Center" Margin="10"/>

        <!-- Filer-->
        <StackPanel Grid.Row="1">

            <!-- Mitarbeiterübersicht -->
            <TextBlock Text="Liste aller Buchungen (nach Datum sortiert): " Margin="10,12" FontSize="17"/>
            <DataGrid AlternatingRowBackground="Lavender"  AutoGenerateColumns="True"  HorizontalAlignment="Left" Width="800" Height="350"
                      ItemsSource="{Binding Buchungen}" VerticalScrollBarVisibility="Visible">
            </DataGrid>

        </StackPanel>
    </Grid>
</UserControl>
